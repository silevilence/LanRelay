# [V2.0] 文件夹结构传输

优先级：P3 (Future)

## 任务细节
*本任务为 V2.0 规划，V1.0 阶段暂不执行。*

1.  **文件树遍历**：
    - 发送端：递归扫描文件夹，生成文件清单列表（相对路径、大小）。

2.  **协议扩展**：
    - 修改文件传输头，增加 `IsDirectory` 标记或 `RelativePath` 字段。
    - 或者是先发送一个 manifest JSON 文件，然后按顺序传输文件流。

3.  **接收端重建**：
    - 根据相对路径自动创建本地文件夹结构。